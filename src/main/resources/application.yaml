spring:
  rabbitmq:
    addresses: 127.0.0.1
  cloud:
    stream:
      bindings:
        balanceIn:
          binder: kafka
          destination: balance
          group: input-group-1
        balanceOut:
          binder: kafka
          destination: balance
          group: output-group-1
      default-binder: kafka
#      kafka:
#        binder:
#          brokers:
#          defaultBrokerPort:
logging:
  level:
    li:
      doerf: DEBUG
  #file: log/application.log

